[module/i3]
type = internal/i3

pin-workspaces = true
index-sort = true

ws-icon-0 = 1;I
ws-icon-1 = 2;II
ws-icon-2 = 3;III
ws-icon-3 = 4;IV
ws-icon-4 = 5;V
ws-icon-5 = 6;VI

format = <label-state><label-mode>

label-mode = %mode%
label-mode-padding = 1
label-mode-background = ${nord.aurora-1}
label-mode-foreground = ${nord.snow-1}

label-focused = %icon%
label-focused-padding = 1
label-focused-background = ${nord.aurora-2}
label-focused-foreground = ${nord.polar-1}

label-unfocused = %icon%
label-unfocused-padding = 1
label-unfocused-background = ${nord.polar-4}
label-unfocused-foreground = ${nord.snow-3}

label-visible = %icon%
label-visible-padding = 1
label-visible-background = ${nord.aurora-5}
label-visible-foreground = ${nord.snow-3}

label-urgent = %icon%
label-urgent-padding = 1
label-urgent-background = ${nord.frost-2}
label-urgent-foreground = ${nord.snow-3}

[module/ewmh]
type = internal/xworkspaces

label-active =
label-active-padding = 1
label-active-background = ${nord.polar-1}
label-active-foreground = ${nord.aurora-2}

label-occupied =
label-occupied-padding = 1
label-occupied-background = ${nord.polar-1}
label-occupied-foreground = ${nord.aurora-2}

label-urgent =
label-urgent-padding = 1
label-urgent-background = ${nord.polar-1}
label-urgent-foreground = ${nord.aurora-1}

label-empty =
label-empty-padding = 1
label-empty-background = ${nord.polar-1}
label-empty-foreground = ${nord.aurora-2}

[module/date]
type = internal/date
interval = 1

date = "%b %d, %Y %a "
time = "%I:%M%p"

format-padding = 1
format-background = ${nord.polar-1}
format-foreground = ${nord.snow-1}

label = %date%%time%
label-underline= ${nord.frost-3}

[module/audio]
type = internal/pulseaudio

sink = alsa_output.pci-0000_03_00.6.analog-stereo

use-ui-max = false

format-volume = <ramp-volume> <label-volume>
format-volume-background = ${nord.aurora-1}
format-volume-padding = 1

label-volume-foreground = ${nord.polar-1}

ramp-volume-0 =
ramp-volume-1 =
ramp-volume-2 =
ramp-volume-foreground = ${nord.polar-1}

label-muted =ﱝ
label-muted-padding = 1
label-muted-foreground = ${nord.polar-1}
label-muted-background = ${nord.aurora-1}

click-right = pavucontrol

[module/battery]
type = internal/battery
battery = BAT1
adapter = ADP1

; This is useful in case the battery never reports 100% charge
full-at = 100

; If an inotify event haven't been reported in this many
; seconds, manually poll for new values.
;
; Needed as a fallback for systems that don't report events
; on sysfs/procfs.
;
; Disable polling by setting the interval to 0.
;
; Default: 5
poll-interval = 2

; see "man date" for details on how to format the time string
; NOTE: if you want to use syntax tags here you need to use %%{...}
; Default: %H:%M:%S
time-format = %H:%M

format-charging-padding = 1
format-charging = <animation-charging>  <label-charging>
format-charging-background = ${nord.aurora-2}
format-charging-foreground = ${nord.polar-1}


format-discharging-padding = 1
format-discharging = <ramp-capacity>  <label-discharging>
format-discharging-background = ${nord.aurora-2}
format-discharging-foreground = ${nord.polar-1}

format-full-prefix-padding = 1

format-full = <label-full>
format-full-prefix = 
format-full-background = ${nord.aurora-2}
format-full-foreground = ${nord.polar-1}

label-charging = %percentage%%

label-discharging = %percentage%%

label-full = "Full "

; Only applies if <ramp-capacity> is used
ramp-capacity-0 = 
ramp-capacity-1 = 
ramp-capacity-2 = 
ramp-capacity-3 = 
ramp-capacity-4 = 

; Only applies if <bar-capacity> is used
;bar-capacity-width = 10

; Only applies if <animation-charging> is used
animation-charging-0 = 
animation-charging-1 = 
animation-charging-2 = 
animation-charging-3 = 
animation-charging-4 = 

; Framerate in milliseconds
animation-charging-framerate = 750

[module/memory-custom]
type = custom/script
interval = 3

exec = "~/.config/polybar/scripts/check-ram-usage.sh"

format = <label>
format-padding = 1
format-foreground = ${nord.polar-1}
format-background = ${nord.aurora-3}

format-prefix = " "
format-prefix-foreground = ${nord.polar-1}

label = "%output%"

[module/wireless-network]
type = internal/network
interface = wlp1s0

label-connected =   %essid%
label-connected-padding = 1
label-connected-foreground = ${nord.polar-1}
label-connected-background = ${nord.aurora-4}

label-disconnected = ✗ disconnected
label-disconnected-padding = 1
label-disconnected-foreground = ${nord.polar-1}
label-disconnected-background = ${nord.aurora-1}

[module/networkspeedup]
type = internal/network
interface = wlp1s0

label-font = 1
label-connected = "%upspeed:7%"

format-connected = <label-connected>
format-connected-padding = 1

format-connected-prefix = " "
format-connected-prefix-foreground = ${nord.polar-1}

format-connected-background = ${nord.frost-2}
format-connected-foreground = ${nord.polar-1}

[module/networkspeeddown]
;https://github.com/jaagr/polybar/wiki/Module:-network
type = internal/network
interface = wlp1s0

label-font = 1
label-connected = "%downspeed:7%"

format-connected = <label-connected>
format-connected-padding = 1

format-connected-prefix = " "
format-connected-prefix-foreground = ${nord.polar-1}

format-connected-background = ${nord.aurora-5}
format-connected-foreground = ${nord.polar-1}

[module/xwindow]
type = internal/xwindow
label = %title:0:25:...%
label-background = ${nord.polar-1}
label-foreground = ${nord.snow-1}
label-underline = ${nord.aurora-3}
label-padding = 1

[module/launcher]
type = custom/text
content =

content-padding = 2
content-background = ${nord.frost-4}
content-foreground = ${nord.snow-1}
